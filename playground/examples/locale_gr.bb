!include "bb://libs/locale/gr"
!τοπικά{|ξεκινάει} ως {|γλώσσα.κείμενο.ξεκινάει}

α = λίστα();
α << 1;
α << 2;
εκτύπωσε(α);
επιβεβαίωσε μήκος(α)==2;

χ = "μια όμορφη μέρα";
εκτύπωσε(χ | ξεκινάει("μια"));

όσο(χ σε διάστημα(0,10)) αν(χ%2==0) εκτύπωσε(χ);

σημείο = νέο {
    χ = 1;
    ψ = 2;
    αλφαριθμητικό() => "(!{αυτό.χ},!{αυτό.ψ})";
}

εκτύπωσε(σημείο);
