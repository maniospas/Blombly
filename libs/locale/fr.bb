// French
// Contributors: 
//  - GPT4.0
//  - Emmanouil Krasanakis (maniospas) - editing

!macro{vecteur::zéro} as {vector::zero}
!macro{vecteur} as {vector}
!macro{liste} as {list}
!macro{imprime} as {print}
!macro{lis} as {read}
!macro{entier} as {int}
!macro{chaîne} as {str}
!macro{additionne} as {add}
!macro{soustrais} as {sub}
!macro{multiplie} as {mul}
!macro{divise} as {div}
!macro{puissance} as {pow}
!macro{logarithme} as {log}
!macro{réel} as {float}
!macro{booléen} as {bool}
!macro{appel} as {call}
!macro{graphiques} as {graphics}
!macro{comme} as {as}
!macro{longueur} as {len}
!macro{tantque(@expr)} as {while(@expr)}
!macro{si(@expr)} as {if(@expr)}
!macro{attrape(@expr)} as {catch(@expr)}
!macro{dans} as {in}
!macro{dans intervalle} as {in range}
!macro{intervalle} as {range}
!macro{itère} as {iter}
!macro{fais} as {do}
!macro{retourne} as {return}
!macro{retire} as {pop}
!macro{insère} as {push}
!macro{suivant} as {next}
!macro{fichier} as {file}
!macro{vide} as {clear}
!macro{déplace} as {move}
!macro{!inclure} as {!include}
!macro{!macro{@expr} comme {@impl}} as {!macro{@expr} as {@impl}}
!macro{!local{@expr} comme {@impl}} as {!local{@expr} as {@impl}}
!macro{!de} as {!of}
!macro{!chaîne} as {!stringify}
!macro{!symbole} as {!symbol}
!macro{!compilation} as {!comptime}
!macro{ceci} as {this}
!macro{temps} as {time}
!macro{aléatoire} as {random}

// standard library
!macro{!noms} as {!namespace}
!macro{!variable} as {!var}
!macro{!anonyme} as {!nameless}
!macro{!avec} as {!with}
!macro{!rassembler} as {gather}
!macro{produis} as {yield}
!macro{défaut} as {default}
!macro{assure} as {assert}

!macro{nouveau{@block}} as {new{@block}}
!macro{langage} as {bb}
!macro{système} as {os}
!macro{transfère} as {transfer}
!macro{texte} as {string}
!macro{joins} as {join}
!macro{commence_par} as {starts}
!macro{finit_par} as {ends}
!macro{indice} as {index}
!macro{sépare} as {split}
!macro{position} as {pos}
!macro{mémoire} as {memory}

//!macro{base_de_données} as {db}
!macro{tableau} as {table}
!macro{valide} as {commit}
!macro{transaction} as {transaction}
!macro{exécute} as {run}

!macro{science} as {sci}
!macro{graphe} as {plot}
//!macro{graphes} as {plots} // TODO: have a different translation for plot/plots than graphics
!macro{Graphe} as {Plot}
!macro{affiche} as {show}
!macro{largeur} as {width}
!macro{hauteur} as {height}
!macro{angle} as {angle}
!macro{couleur} as {color}
!macro{titre} as {title}
!macro{police} as {font}
!macro{axes} as {axes}
!macro{ombrage} as {shade}
!macro{texte} as {text}

!macro{journal} as {logger}
!macro{ok} as {ok}
!macro{info} as {info}
!macro{échec} as {fail}
!macro{avertis} as {warn}
