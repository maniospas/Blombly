// German
// Contributors: 
//  - GPT4.0
//  - Emmanouil Krasanakis (maniospas) - editing

!macro{vektor::nullen} as {vector::zero}  // adjusted to ot confuse with typical programming null
!macro{vektor} as {vector}
!macro{liste} as {list}
!macro{drucke} as {print}
!macro{lies} as {read}
!macro{ganzzahl} as {int}
!macro{zeichenkette} as {str}
!macro{addiere} as {add}
!macro{subtrahiere} as {sub}
!macro{multipliziere} as {mul}
!macro{dividiere} as {div}
!macro{potenz} as {pow}
!macro{logarithmus} as {log}
!macro{fließkommazahl} as {float}
!macro{boolesch} as {bool}
!macro{aufruf} as {call}
!macro{grafiken} as {graphics}
!macro{als} as {as}
!macro{länge} as {len}
!macro{solange(@expr)} as {while(@expr)}
!macro{wenn(@expr)} as {if(@expr)}
!macro{fange(@expr)} as {catch(@expr)}
//!macro{in} as {in}
!macro{im bereich} as {in range}
!macro{bereich} as {range}
!macro{iteriere} as {iter}
!macro{mache} as {do}
!macro{zurück} as {return}
!macro{entferne} as {pop}
!macro{füge_hinzu} as {push}
!macro{nächstes} as {next}
!macro{datei} as {file}
!macro{leere} as {clear}
!macro{verschiebe} as {move}
!macro{!einfügen} as {!include}
!macro{!makro{@expr} als {@impl}} as {!macro{@expr} as {@impl}}
!macro{!lokal{@expr} als {@impl}} as {!local{@expr} as {@impl}}
!macro{!von} as {!of}
!macro{!zeichenkette} as {!stringify}
!macro{!symbol} as {!symbol}
!macro{!kompilierzeit} as {!comptime}
!macro{dies} as {this}
!macro{zeit} as {time}
!macro{zufall} as {random}

// standard library
!macro{!namensraum} as {!namespace}
!macro{!variable} as {!var}
!macro{!namenlos} as {!nameless}
!macro{!mit} as {!with}
!macro{!sammle} as {gather}
!macro{liefere} as {yield}
!macro{standard} as {default}
!macro{prüfe} as {assert}

!macro{neu{@block}} as {new{@block}}
!macro{sprache} as {bb}
!macro{betriebssystem} as {os}
!macro{übertrage} as {transfer}
!macro{text} as {string}
!macro{verbinde} as {join}
!macro{beginnt_mit} as {starts}
!macro{endet_mit} as {ends}
!macro{index} as {index}
!macro{teile} as {split}
!macro{position} as {pos}
!macro{arbeitsspeicher} as {memory}

!macro{datenbank} as {db}
!macro{tabelle} as {table}
!macro{bestätige} as {commit}
!macro{transaktion} as {transaction}
!macro{führe_aus} as {run}

!macro{wissenschaft} as {sci}
!macro{diagramm} as {plot}
!macro{diagramme} as {plots}
!macro{Diagramm} as {Plot}
!macro{zeige} as {show}
!macro{breite} as {width}
!macro{höhe} as {height}
!macro{winkel} as {angle}
!macro{farbe} as {color}
!macro{titel} as {title}
!macro{schriftart} as {font}
!macro{achsen} as {axes}
!macro{schattierung} as {shade}
!macro{text} as {text}

!macro{protokoll} as {logger}
!macro{ok} as {ok}
!macro{info} as {info}
!macro{fehler} as {fail}
!macro{warnung} as {warn}
