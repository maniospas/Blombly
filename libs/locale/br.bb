// Brazilian: produced with GPT4.0

!macro{vetor::zero} as {vector::zero}
!macro{vetor} as {vector}
!macro{lista} as {list}
!macro{imprima} as {print}
!macro{leia} as {read}
!macro{inteiro} as {int}
!macro{texto} as {str}
!macro{some} as {add}
!macro{subtraia} as {sub}
!macro{multiplique} as {mul}
!macro{divida} as {div}
!macro{potência} as {pow}
!macro{logaritmo} as {log}
!macro{real} as {float}
!macro{booleano} as {bool}
!macro{chamada} as {call}
!macro{gráficos} as {graphics}
!macro{como} as {as}
!macro{tamanho} as {len}
!macro{enquanto(@expr)} as {while(@expr)}
!macro{se(@expr)} as {if(@expr)}
!macro{capture(@expr)} as {catch(@expr)}
!macro{em} as {in}
!macro{em intervalo} as {in range}
!macro{intervalo} as {range}
!macro{iterar} as {iter}
!macro{faça} as {do}
!macro{retorne} as {return}
!macro{retire} as {pop}
!macro{insira} as {push}
!macro{próximo} as {next}
!macro{arquivo} as {file}
!macro{limpe} as {clear}
!macro{mova} as {move}
!macro{!inclua} as {!include}
!macro{!macro{@expr} como {@impl}} as {!macro{@expr} as {@impl}}
!macro{!local{@expr} como {@impl}} as {!local{@expr} as {@impl}}
!macro{!de} as {!of}
!macro{!para_string} as {!stringify}
!macro{!símbolo} as {!symbol}
!macro{!tempo_de_compilação} as {!comptime}
!macro{isto} as {this}
!macro{tempo} as {time}
!macro{aleatório} as {random}

// standard library
!macro{!espaço_de_nomes} as {!namespace}
!macro{!variável} as {!var}
!macro{!anônimo} as {!nameless}
!macro{!com} as {!with}
!macro{!colete} as {gather}
!macro{produza} as {yield}
!macro{padrão} as {default}
!macro{afirme} as {assert}

!macro{novo{@block}} as {new{@block}}
!macro{linguagem} as {bb}
!macro{sistema_operacional} as {os}
!macro{transfira} as {transfer}
!macro{texto} as {string}
!macro{una} as {join}
!macro{começa} as {starts}
!macro{termina} as {ends}
!macro{índice} as {index}
!macro{divida} as {split}
!macro{posição} as {pos}
!macro{memória} as {memory}

//!macro{banco_de_dados} as {db}
!macro{tabela} as {table}
!macro{confirme} as {commit}
!macro{transação} as {transaction}
!macro{execute} as {run}

!macro{ciência} as {sci}
!macro{gráfico} as {plot}
//!macro{gráficos} as {plots} // TODO: have a different translation for plot/plots than graphics
!macro{Gráfico} as {Plot}
!macro{mostre} as {show}
!macro{largura} as {width}
!macro{altura} as {height}
!macro{ângulo} as {angle}
!macro{cor} as {color}
!macro{título} as {title}
!macro{fonte} as {font}
!macro{eixos} as {axes}
!macro{sombreamento} as {shade}
!macro{texto} as {text}

!macro{registro} as {logger}
!macro{ok} as {ok}
!macro{informação} as {info}
!macro{erro} as {fail}
!macro{avise} as {warn}
