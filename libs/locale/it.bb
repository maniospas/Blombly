// Italian
// Contributors: 
//  - GPT4.0
//  - Emmanouil Krasanakis (maniospas) - editing

!macro{vettore::zero} as {vector::zero}
!macro{vettore} as {vector}
!macro{lista} as {list}
!macro{stampa} as {print}
!macro{leggi} as {read}
!macro{intero} as {int}
!macro{stringa} as {str}
!macro{somma} as {add}
!macro{sottrai} as {sub}
!macro{moltiplica} as {mul}
!macro{dividi} as {div}
!macro{potenza} as {pow}
!macro{logaritmo} as {log}
!macro{reale} as {float}
!macro{booleano} as {bool}
!macro{chiamata} as {call}
!macro{grafica} as {graphics}
!macro{come} as {as}
!macro{lunghezza} as {len}
!macro{mentre(@expr)} as {while(@expr)}
!macro{se(@expr)} as {if(@expr)}
!macro{cattura(@expr)} as {catch(@expr)}
!macro{in} as {in}
!macro{in intervallo} as {in range}
!macro{intervallo} as {range}
!macro{itera} as {iter}
!macro{esegui} as {do}
!macro{ritorna} as {return}
!macro{estrai} as {pop}
!macro{inserisci} as {push}
!macro{prossimo} as {next}
!macro{file} as {file}
!macro{svuota} as {clear}
!macro{sposta} as {move}
!macro{!includi} as {!include}
!macro{!macro{@expr} come {@impl}} as {!macro{@expr} as {@impl}}
!macro{!locale{@expr} come {@impl}} as {!local{@expr} as {@impl}}
!macro{!di} as {!of}
!macro{!stringa} as {!stringify}
!macro{!simbolo} as {!symbol}
!macro{!compilazione} as {!comptime}
!macro{questo} as {this}
!macro{casuale} as {random}
!macro{tempo} as {time}

// standard library
!macro{!nomi} as {!namespace}
!macro{!variabile} as {!var}
!macro{!anonimo} as {!nameless}
!macro{!con} as {!with}
!macro{!raccogli} as {gather}
!macro{produci} as {yield}
!macro{predefinito} as {default}
!macro{assicura} as {assert}

!macro{nuovo{@block}} as {new{@block}}
!macro{linguaggio} as {bb}
!macro{sistema_operativo} as {os}
!macro{trasferisci} as {transfer}
!macro{testo} as {string}
!macro{unisci} as {join}
!macro{inizia_con} as {starts}
!macro{termina_con} as {ends}
!macro{indice} as {index}
!macro{dividi} as {split}
!macro{posizione} as {pos}
!macro{memoria} as {memory}

!macro{database} as {db}
!macro{tabella} as {table}
!macro{conferma} as {commit}
!macro{transazione} as {transaction}
!macro{esegui} as {run}

!macro{scienza} as {sci}
!macro{grafico} as {plot}
!macro{grafici} as {plots}
!macro{Grafico} as {Plot}
!macro{mostra} as {show}
!macro{larghezza} as {width}
!macro{altezza} as {height}
!macro{angolo} as {angle}
!macro{colore} as {color}
!macro{titolo} as {title}
!macro{carattere} as {font}
!macro{assi} as {axes}
!macro{ombreggiatura} as {shade}
!macro{testo} as {text}

!macro{registro} as {logger}
!macro{ok} as {ok}
!macro{informazione} as {info}
!macro{errore} as {fail}
!macro{avviso} as {warn}
